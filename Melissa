/* prüfen, ob das Wort vollständig erraten Wurde --> wenn Methode 'true' zurückgibt = gewonnen
            * hinweis zum sieg für den Spieler und dann wird das spiel beendet mit einem break */

            if (WortRaten(BuchstabeRaten)) {
                System.out.println("Herzlichen Glückwunsch, du hast gewonnen! Das Wort ist: " + WortRaten);
                break;
            }
        }
        /* prüfen, ob max. anzahl an Fehlversuche erreicht wurde --> wenn ja hinweis zur Niederlage für den Spieler
        mit dem Lösungswort */

        if (Fehlversuche == 10) {
            System.out.println("Du hast leider verloren. Das richtige Wort wäre gewesen: " + WortRaten);
        }

        scanner.close();
    }

//innerhalb der main methode 
